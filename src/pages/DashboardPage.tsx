export function DashboardPage() {
  return (
    <section className="rounded-4xl bg-grayscale-50 px-8 py-10">
      <p className="text-helper-medium uppercase tracking-[0.18em] text-purple-500">
        Dashboard
      </p>
      <h1 className="mt-4 text-h1">Main landing page</h1>
      <p className="mt-4 max-w-160 text-body-s text-grayscale-600">
        This is the dashboard route for now. Next we can replace this simple
        placeholder with the real homepage sections from the design.
      </p>
    </section>
  );
}
